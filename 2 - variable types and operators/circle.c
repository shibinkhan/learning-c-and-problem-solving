/**

**/

#include<stdio.h>

int main() {
    /*
    printf("Enter a radius: ");
    double radius;
    scanf("%lf", &radius);

    const double PI = 3.1416;

    double parimiter = 2 * PI * radius;
    printf("Parimiter is %.2lf\n", parimiter);

    double area = PI * (radius * radius);
    printf("Area is %.2lf\n", area);
    */

    int a = 5, b = 2;
	double q = (double)a/(double)b;
	printf("%lf", q);

	return 0;
}