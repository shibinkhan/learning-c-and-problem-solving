/*
#include<stdio.h>

int main() {
    int s, t, x;
    scanf("%d %d %d", &s, &t, &x);

    // s
    int go;
    if (s==0) {
        go = 24 * 60;
    }
    else {
        go = s * 60;
    }

    // t
    int come;
    if (t==0) {
        come = 24 * 60;
    }
    else {
        come = t * 60;
    }

    // x
    int x2;
    if (x==0) {
        x2 = (24 * 60) + 30;
    }
    else {
        x2 = (x * 60) + 30;
    }

    // final
    if (go < x2 && come > x2) {
        printf("Yes");
    }
    else {
        printf("No");
    }

    return 0;
}
*/

#include<stdio.h>

int main() {
    int s, t, x;
    scanf("%d %d %d", &s, &t, &x);



    return 0;
}
