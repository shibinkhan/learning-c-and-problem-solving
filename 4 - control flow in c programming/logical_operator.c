#include<stdio.h>
#include<stdbool.h>

int main() {
    int a;
    scanf("%d", &a);

    /*
    // and = &&
    if(a > 0 && a > 10) {
        printf("true\n");
    }
    else {
        printf("false\n");
    }
    */


    // or = ||
    if(a == a || a == e || a == i || a == o || a == u) {
        printf("vawel\n");
    }
    else {
        printf("consonant\n");
    }



    // not = !


    return 0;
}
