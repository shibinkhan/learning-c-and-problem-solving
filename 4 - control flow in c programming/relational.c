// < <= >= > == !=

#include<stdio.h>
#include<stdbool.h>

int main() {
    int a = 28, b = 18;

    /*
    if(a < b) {
        printf("a < b\n");
    }

    if(a <= b) {
        printf("a <= b\n");
    }

    if(a > b) {
        printf("a > b\n");
    }

    if(a >= b) {
        printf("a >= b\n");
    }

    if(a == b) {
        printf("a == b\n");
    }

    if(a != b) {
        printf("a != b\n");
    }

    if(a*2 == b-2) {
        printf("a == b\n");
    }

    int comp = a < b;
    printf("%d\n", comp);

    if(comp) {
        printf("true");
    }
    else {
        printf("false");
    }
    */

    bool condition = true;
    printf("%d", condition);

    return 0;
}